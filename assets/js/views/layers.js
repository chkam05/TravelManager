document.addEventListener('travel-manager:views-ready',()=>{
 const t=window.i18n.t,locale=window.i18n.locale.replace('_','-');
 const form=document.querySelector('#layers-search'),input=document.querySelector('#layers-search-input'),sortButton=document.querySelector('#layers-sort-button'),sortMenu=document.querySelector('#layers-sort-menu'),list=document.querySelector('[data-layers-list]');
 if(!form||!input||!sortButton||!sortMenu||!list)return;
 const state={layers:[],query:'',sortDirection:'asc'};
 const name=layer=>layer.name||t('LAYER_EDITOR.NEW_LAYER',{number:''});
 const filtered=()=>{const query=state.query.trim().toLocaleLowerCase(locale);const items=query?state.layers.filter(layer=>name(layer).toLocaleLowerCase(locale).includes(query)):[...state.layers];return items.sort((a,b)=>{const result=name(a).localeCompare(name(b),locale,{sensitivity:'base'});return state.sortDirection==='asc'?result:-result})};
 const setSortOpen=open=>{sortMenu.setAttribute('aria-hidden',String(!open));sortButton.setAttribute('aria-expanded',String(open))};
 const refresh=async()=>{state.layers=await window.travelManagerCustomLayers.list();render()};
 const edit=layer=>{window.travelManagerNavigation?.showView('map')||document.querySelector('[data-navigation-view="map"]')?.click();window.travelManagerLayerEditor.open(layer)};
 const rename=async layer=>{const value=await window.travelManagerLayerNameEditor?.show({name:name(layer),icon:layer.icon||'',showTitle:layer.show_title===true});if(!value)return;await window.travelManagerCustomLayers.save({...layer,name:value.name,icon:value.icon,show_title:value.showTitle});document.dispatchEvent(new CustomEvent('travel-manager:custom-layers-changed'))};
 const remove=async layer=>{if(await window.travelManagerDialogs.yesNo({title:t('LAYER_EDITOR.DELETE_TITLE'),description:t('LAYER_EDITOR.DELETE_TEXT')})){await window.travelManagerCustomLayers.delete(layer.id);document.dispatchEvent(new CustomEvent('travel-manager:custom-layers-changed'))}};
 const action=(label,icon,handler,danger=false)=>{const button=document.createElement('button');button.className=`layers-view__action${danger?' layers-view__action--danger':''}`;button.type='button';button.innerHTML=`<i data-lucide="${icon}" aria-hidden="true"></i><span>${label}</span>`;button.onclick=handler;return button};
 function render(){list.replaceChildren();const items=filtered();if(!items.length){const empty=document.createElement('p');empty.className='layers-view__empty';empty.textContent=state.query?t('LAYERS_VIEW.NO_RESULTS'):t('LAYERS_VIEW.EMPTY');list.append(empty);return}items.forEach(layer=>{const row=document.createElement('article');row.className='layers-view__item';const icon=document.createElement('div');icon.className='layers-view__icon';if(layer.icon)icon.textContent=layer.icon;else icon.innerHTML='<i data-lucide="layers" aria-hidden="true"></i>';const copy=document.createElement('div');copy.className='layers-view__copy';const title=document.createElement('div');title.className='layers-view__name';title.textContent=name(layer);title.title=name(layer);const count=document.createElement('div');count.className='layers-view__count';count.textContent=t('LAYERS_VIEW.ELEMENT_COUNT',{count:(layer.elements||[]).length});copy.append(title,count);row.append(icon,copy,action(t('LAYER_EDITOR.RENAME'),'pencil',()=>rename(layer)),action(t('COMMON.EDIT'),'square-pen',()=>edit(layer)),action(t('COMMON.DELETE'),'trash-2',()=>remove(layer),true));list.append(row)});window.lucide?.createIcons({attrs:{'stroke-width':2}})}
 form.onsubmit=event=>{event.preventDefault();state.query=input.value;render()};input.oninput=()=>{state.query=input.value;render()};sortButton.onclick=()=>setSortOpen(sortMenu.getAttribute('aria-hidden')!=='false');sortMenu.onclick=event=>{const button=event.target.closest('[data-sort-direction]');if(!button)return;state.sortDirection=button.dataset.sortDirection;setSortOpen(false);render()};document.addEventListener('click',event=>{if(!sortMenu.contains(event.target)&&!sortButton.contains(event.target))setSortOpen(false)});document.querySelector('[data-layer-add]')?.addEventListener('click',()=>{window.travelManagerNavigation?.showView('map')||document.querySelector('[data-navigation-view="map"]')?.click();window.travelManagerLayerEditor.open()});document.addEventListener('travel-manager:custom-layers-changed',refresh);refresh().catch(()=>render());
});
